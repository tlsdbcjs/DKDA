apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-windows-smb
  namespace: default
  labels:
    app: windows-smb
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client-default